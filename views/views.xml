<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="remiseglobale.list">
      <field name="name">remiseglobale list</field>
      <field name="model">remiseglobale.remiseglobale</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="remiseglobale.action_window">
      <field name="name">remiseglobale window</field>
      <field name="res_model">remiseglobale.remiseglobale</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="remiseglobale.action_server">
      <field name="name">remiseglobale server</field>
      <field name="model_id" ref="model_remiseglobale_remiseglobale"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="remiseglobale" id="remiseglobale.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="remiseglobale.menu_1" parent="remiseglobale.menu_root"/>
    <menuitem name="Menu 2" id="remiseglobale.menu_2" parent="remiseglobale.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="remiseglobale.menu_1_list" parent="remiseglobale.menu_1"
              action="remiseglobale.action_window"/>
    <menuitem name="Server to list" id="remiseglobale" parent="remiseglobale.menu_2"
              action="remiseglobale.action_server"/>
    -->
  </data>
</odoo>